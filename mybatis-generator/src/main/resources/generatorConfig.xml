<!DOCTYPE generatorConfiguration PUBLIC
        "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">
<generatorConfiguration>

    <!-- 可以导入数据库的配置信息，这样就不用写到生成xml文件中了 -->
    <properties resource="config.properties"/>

    <!-- MyBatis3Simple风格，还可以使用MyBatis3， targetRuntime=“MyBatis3” -->
    <!-- targetRuntime="MyBatis3Simple" 或 targetRuntime="MyBatis3"-->
    <context id="Mysql" targetRuntime="MyBatis3Simple">

        <!-- 添加分隔符 -->
        <property name="beginningDelimiter" value="`"/>
        <property name="endingDelimiter" value="`"/>

        <property name="javaFileEncoding" value="UTF-8"/>

        <!--生成mapper.xml时覆盖原文件-->
        <plugin type="org.mybatis.generator.plugins.UnmergeableXmlMappersPlugin"/>

        <commentGenerator>
            <!-- 是否去除自动生成的注释 true：是 ： false:否。 自动生成注释太啰嗦，可以编码扩展CommentGenerator -->
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>

        <!-- 数据库连接 -->
        <jdbcConnection driverClass="${jdbc.driverClass}"
                        connectionURL="${jdbc.url}"
                        userId="${jdbc.user}"
                        password="${jdbc.password}">

            <!-- 设置为true就只读取db_book下的表, 否则会优先读取到mysql的user表  -->
            <property name="nullCatalogMeansCurrent" value="true"/>
        </jdbcConnection>

        <!-- 如果使用java代码生成的时候，需要在targetProject配置下添加子项目名，否则会在父项目下找对应的java和resource文件夹，生成到父项目下 -->

        <!-- 生成PO的包名和位置 -->
        <javaModelGenerator targetPackage="${package.name}.entity" targetProject="src/main/java"/>

        <!-- 生成XML映射文件的包名和位置 -->
        <sqlMapGenerator targetPackage="${package.name}.mapper" targetProject="src/main/resources"/>

        <!-- 生成Mapper接口的包名和位置 -->
        <javaClientGenerator type="XMLMAPPER" targetPackage="${package.name}.mapper" targetProject="src/main/java"/>

        <!-- 要生成对应表配置 -->
        <table tableName="t_pay" domainObjectName="Pay">
            <!-- 自增主键列 -->
            <generatedKey column="id" sqlStatement="MYSQL" identity="true"/>
            <!-- tinyint映射为Integer -->
<!--            <columnOverride column="role" javaType="Integer" jdbcType="TINYINT"/>-->
        </table>

        <!-- 还可以配置通配符，表示所有的表 -->
        <table tableName="%" >

        </table>
    </context>
</generatorConfiguration>
